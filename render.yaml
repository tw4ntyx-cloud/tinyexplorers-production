services:
  - type: web
    name: tiny-explorers-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    envVars:
      - key: MONGO_URL
        sync: false
      - key: DB_NAME
        value: tiny_explorers
      - key: RESEND_API_KEY
        sync: false
      # Canonical Tiny Explorers address for this deployment: hello@tinyexplorersbda.com
      # (staff notification recipient). Set manually in the Render dashboard.
      - key: ADMIN_EMAIL
        sync: false
      # Sending identity — recommended default is also hello@tinyexplorersbda.com,
      # but its domain must be verified in Resend before production sending works.
      - key: FROM_EMAIL
        sync: false
      # Public website URL used in outgoing email links. Set to the current
      # GitHub Pages review URL for pre-launch testing; change to
      # https://tinyexplorersbda.com at launch — no code changes required.
      - key: WEBSITE_URL
        sync: false
      - key: ADMIN_API_KEY
        sync: false
      - key: CORS_ORIGINS
        sync: false
      - key: CORS_ALLOW_CREDENTIALS
        value: "false"
      # NOTE: Render is not a Google Cloud environment, so it cannot provide
      # Application Default Credentials (keyless auth) for Google Workspace
      # out of the box — only GOOGLE_SERVICE_ACCOUNT_JSON (a downloaded key)
      # would work here, which is unavailable if your Google Cloud org
      # enforces iam.managed.disableServiceAccountKeyCreation. See
      # docs/google-workspace-newsletter.md for the keyless-friendly hosting
      # comparison (e.g. Google Cloud Run) if that policy applies to you.
      - key: GOOGLE_SERVICE_ACCOUNT_EMAIL
        sync: false
      - key: GOOGLE_SERVICE_ACCOUNT_JSON
        sync: false
      - key: GOOGLE_WORKSPACE_ADMIN_EMAIL
        sync: false
      - key: GOOGLE_NEWSLETTER_GROUP_EMAIL
        sync: false
      - key: ENVIRONMENT
        value: production
      - key: LOG_LEVEL
        value: INFO
